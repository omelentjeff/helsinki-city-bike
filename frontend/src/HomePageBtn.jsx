import * as React from "react";
import Stack from "@mui/material/Stack";
import Button from "@mui/material/Button";

export default function HomePageBtn({ content }) {
  return <Button variant="contained">{content}</Button>;
}
